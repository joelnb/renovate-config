name: Renovate

on:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: '*/15 * * * *'

jobs:
  renovate:
    name: Run Renovate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3.1.0
      - name: Renovate
        uses: renovatebot/github-action@v32.241.9
        with:
          configurationFile: renovate.json
          token: ${{ secrets.RENOVATE_TOKEN }}
